# Architecture — Driver Order Routing App

No architecture yet. Technical Lead will define this after Product Owner creates the MVP backlog.

## Initial Technical Considerations
- Mobile-first app for drivers and admins.
- Backend API for orders, drivers, assignments, route optimization, authentication, and status updates.
- Route optimization likely needs geocoding and route-distance estimation.
- MVP can start with heuristic optimization before advanced VRP solvers.
