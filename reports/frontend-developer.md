Project name: Driver Order Routing

The achievements for the round:
- Added `apiClient` wrapper in `repo/frontend/app.js` for `/excel-template`, `/orders/import/excel`, and `/import-batches/{id}`
- Frontend prototype tests pass
- Frontend continues static prototype coverage for admin and driver views

Blockers if exist:
- API-backed wiring beyond `apiClient` stub still pending

What will be next:
- React/Vite PWA scaffold and API-backed flow wiring
- Mobile viewport testing
- Excel import row-error review screen
